# Refactor pass 06-10

Implemented:
- isolated runtime state primitives for agent decomposition;
- introduced tool registry boundary;
- added frontend selectors boundary.

The next migration can move behavior incrementally without changing public APIs.
